name: Build and Analyze with CodeQL

on:
  push:
    branches:
      - UX-dev
  pull_request:
    branches:
      - "*"

jobs:
  build:
    name: Build with Maven
    runs-on: ubuntu-22.04  # Actualiza esta versión si fuera necesario

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Usa JDK 23 de adoptium
      - name: Set up JDK 23
        uses: actions/setup-java@v3
        with:
          java-version: '23'
          distribution: 'adoptopenjdk'  # Usa AdoptOpenJDK (ahora Eclipse Temurin)

      - name: Build with Maven
        run: mvn clean install -DskipTests

      - name: Run tests
        run: mvn clean test

      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@v2
